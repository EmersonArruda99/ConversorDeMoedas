public class Mensagem {

    public void convertida(String de, String para, double deValor, Moeda paraValor){
        System.out.println("\n Convertendo " + deValor + "[" + de.toUpperCase() + "]" + " para [" + para.toUpperCase() + "] temos o valor de "
                + paraValor + ".\n");
    }
}
